{
  "name": "vintone-studio",
  "version": "0.1.0",
  "description": "Codex local plugin shell for copy-first VINTONE Photoshop planning and runbooks.",
  "type": "module",
  "private": true,
  "bin": {
    "vintone-studio": "./src/cli.mjs"
  },
  "scripts": {
    "check": "node --check src/server.mjs && node --check src/cli.mjs && find uxp-companion/src -name '*.js' -print0 | xargs -0 -n1 node --check && sh -n scripts/start-mcp.sh && node -e \"JSON.parse(require('fs').readFileSync('.codex-plugin/plugin.json','utf8')); JSON.parse(require('fs').readFileSync('.mcp.json','utf8')); JSON.parse(require('fs').readFileSync('uxp-companion/manifest.json','utf8'))\" && node ./src/cli.mjs check --json",
    "test": "node --test tests/*.test.mjs"
  },
  "keywords": [
    "codex",
    "vintone",
    "photoshop",
    "computer-use",
    "screen-printing"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
